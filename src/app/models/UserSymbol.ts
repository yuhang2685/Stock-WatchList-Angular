export class UserSymbol {

    id:         number;
    userName:   string;
    quote:      string;

    constructor(userName : string, quote : string) {
      this.id = 1;
      this.userName = userName;
      this.quote = quote;
    }
  }
  
  